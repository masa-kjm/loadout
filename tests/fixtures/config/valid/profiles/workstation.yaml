schema_version: 2
id: workstation
resources:
  git-config:
    type: file
    properties:
      kind: file
      source:
        store: dotfiles
        path: gitconfig
      target: ~/.gitconfig
      operation: link
